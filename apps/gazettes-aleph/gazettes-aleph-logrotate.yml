---
- hosts:
    - gazettes_aleph
  become: yes

  tasks:
    - name: "Ensure logrotate config exists"
      template:
        src: logrotate-aleph
        dest: /etc/logrotate.d/aleph
        mode: 0644
