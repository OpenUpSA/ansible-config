---
hosts:
    - open-justice
  become: yes
​
tasks:
​
  - name: Install git
    apt:
      name: git
      state: present
      update_cache: yes
  
  - name: clone Aleph repo
    become: yes
    git:
      repo: 'https://github.com/alephdata/aleph.git'
      dest: '/var/open-justice/aleph'
​